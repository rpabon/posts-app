import React, { PureComponent } from 'react';

/**
|---------------------------------------------------------
| The loader must be a react component for react-loadable
|---------------------------------------------------------
*/

export default class Loading extends PureComponent {
  render() {
    return <div>...Loading</div>;
  }
}
